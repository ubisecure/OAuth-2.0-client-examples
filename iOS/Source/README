An example OAuth client that displays some user information

The configuration is defined in Config.h

To install:

sudo gem install cocoapods

pod init
open OAuthClient.xcworkspace

To run on an iOS device without joining iOS Developer Program, use Xcode 7.0beta or later. In project settings Identity section, add your Apple Id to the Team field.
